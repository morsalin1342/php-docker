; Routes PHP's mail() through msmtp (msmtp-mta provides the /usr/sbin/sendmail
; this points at). msmtp itself ships unconfigured — /etc/msmtprc is supplied
; by whatever deploys this image, not baked in at build time.
sendmail_path = "/usr/sbin/sendmail -t -i"
